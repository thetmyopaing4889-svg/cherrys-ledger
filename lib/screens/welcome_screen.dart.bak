import 'package:flutter/material.dart';
import 'boss_list_screen.dart';

class WelcomeScreen extends StatelessWidget {
  const WelcomeScreen({super.key});

  static const _cherryDark = Color(0xFF9F1239);
  static const _bg = Color(0xFFFDF2F8);

  @override
  Widget build(BuildContext context) {
    final topPad = MediaQuery.of(context).padding.top;

    return Scaffold(
      backgroundColor: _bg,
      body: Stack(
        fit: StackFit.expand,
        children: [
          // Background Image
          Image.asset('assets/images/welcome.png', fit: BoxFit.cover),

          // Soft overlay so text is readable (not too dark)
          Container(color: Colors.black.withOpacity(0.18)),

          // TOP TEXT: "မိန်းမကို အရမ်းချစ်တယ် အာဘွား"
          Positioned(
            left: 20,
            right: 20,
            top: topPad + 88, // <- adjust if you want higher/lower
            child: Center(
              child: Text(
                'မိန်းမကို အရမ်းချစ်တယ် အာဘွား 😘',
                textAlign: TextAlign.center,
                style: TextStyle(
                  fontSize: 22,
                  fontWeight: FontWeight.w800,
                  color: Colors.white,
                  letterSpacing: 0.2,
                  shadows: [
                    // Glow effect
                    Shadow(
                      color: Colors.white.withOpacity(0.85),
                      blurRadius: 14,
                    ),
                    Shadow(
                      color: Colors.white.withOpacity(0.55),
                      blurRadius: 28,
                    ),
                    // Depth
                    Shadow(
                      color: Colors.black.withOpacity(0.45),
                      blurRadius: 8,
                      offset: const Offset(0, 2),
                    ),
                  ],
                ),
              ),
            ),
          ),

          // Bottom Button
          Positioned(
            left: 20,
            right: 20,
            bottom: 26,
            child: SafeArea(
              top: false,
              child: SizedBox(
                height: 58,
                child: ElevatedButton(
                  style: ElevatedButton.styleFrom(
                    backgroundColor: _cherryDark, // cherry color ✅
                    foregroundColor: Colors.white,
                    elevation: 10,
                    shadowColor: _cherryDark.withOpacity(0.35),
                    shape: RoundedRectangleBorder(
                      borderRadius: BorderRadius.circular(18),
                    ),
                  ),
                  onPressed: () {
                    Navigator.pushReplacement(
                      context,
                      MaterialPageRoute(builder: (_) => const BossListScreen()),
                    );
                  },
                  child: const Row(
                    mainAxisAlignment: MainAxisAlignment.center,
                    children: [
                      Text(
                        'Enter Ledger',
                        style: TextStyle(
                          fontSize: 18,
                          fontWeight: FontWeight.w900,
                        ),
                      ),
                      SizedBox(width: 10),
                      Icon(Icons.arrow_forward_rounded),
                    ],
                  ),
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
